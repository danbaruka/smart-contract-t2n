import { ForceFrame } from "./ForceFrame.js";
import { LApp } from "./LApp.js";
import { RApp } from "./RApp.js";
export type Frame = ForceFrame | LApp | RApp;
export declare function isFrame(stuff: any): stuff is Frame;
export declare class CEKFrames {
    private _frames;
    constructor(init?: Frame[]);
    get isEmpty(): boolean;
    callStack(): string[];
    push(f: Frame): void;
    pop(): Frame;
    clone(): CEKFrames;
    _unsafe_clear(): void;
}
export declare function showFrames(frames: Readonly<CEKFrames>): string;
