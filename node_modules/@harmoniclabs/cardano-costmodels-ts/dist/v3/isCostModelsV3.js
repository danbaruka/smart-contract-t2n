"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isCostModelsV3 = void 0;
var ints_1 = require("../utils/ints.js");
var costModelV3Keys_1 = require("./costModelV3Keys.js");
var N_COST_MODEL_PLUTUS_V3_1 = require("./N_COST_MODEL_PLUTUS_V3.js");
function isCostModelsV3(something) {
    if (!(typeof something === "object" && something !== null))
        return false;
    if (Array.isArray(something)) {
        // CostModelPlutusV3Array
        return (something.length >= N_COST_MODEL_PLUTUS_V3_1.N_COST_MODEL_PLUTUS_V3 || something.length >= N_COST_MODEL_PLUTUS_V3_1.N_COST_MODEL_PLUTUS_V3_CHANG_1) &&
            something.every(ints_1.canBeInteger);
    }
    var keys = Object.keys(something);
    if (keys.length < N_COST_MODEL_PLUTUS_V3_1.N_COST_MODEL_PLUTUS_V3_CHANG_1)
        return false;
    if (keys.length < N_COST_MODEL_PLUTUS_V3_1.N_COST_MODEL_PLUTUS_V3) {
        // CostModelPlutusV3Array ( chang )
        return costModelV3Keys_1.costModelChangV3Keys.every(function (k) {
            var val = something[k];
            return val !== undefined && (0, ints_1.canBeInteger)(val);
        });
    }
    return (
    // CostModelPlutusV3
    costModelV3Keys_1.costModelV3Keys.every(function (k) {
        var val = something[k];
        return val !== undefined && (0, ints_1.canBeInteger)(val);
    }));
}
exports.isCostModelsV3 = isCostModelsV3;
