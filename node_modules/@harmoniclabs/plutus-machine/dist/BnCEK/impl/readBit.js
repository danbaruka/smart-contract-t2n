"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.readBit = void 0;
var CEKValue_1 = require("../../CEKValue/index.js");
function readBit(bs, i) {
    var byteLen = bs.length;
    var nBits = byteLen * 8;
    if (i < 0 || i >= nBits)
        return new CEKValue_1.CEKError("readBit :: index out of bounds", { i: i, nBits: nBits });
    // i == 0 is the LAST bit of the LAST byte
    // i == nBits - 1 is the FIRST bit of the FIRST byte
    var iByte = Math.floor(i / 8);
    var iBit = i % 8;
    var byte = bs[bs.length - 1 - iByte];
    return ((byte >> iBit) & 1) === 1;
}
exports.readBit = readBit;
